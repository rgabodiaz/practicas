# Evalua un programa que ejecute un código con las
# siguientes condiciones:
# Si es divisible entre tres, sustituye el valor por "fizz"
# Si es divisible entre cinco, sustituye el valor por "buzz"
# Si es divisible entre ambos, sustituye por "fizzbuzz"

# Cree un bucle for, para evaluar desde el número uno al cien

for i in range(1, 101):
    if i % 3 == 0 and i % 5 == 0:
        print("fizzbuzz")
        continue
    if i % 3 == 0:
        print("fizz")
        continue
    if i % 5 == 0:
        print("buzz")
        continue
    else:
        print(i)
